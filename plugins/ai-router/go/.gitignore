# Build artifacts — never committed. The server is built from source on first
# use by run.sh (build-on-install model); optional prebuilt release binaries
# go under dist/ via `make dist`.
/ai-router
/.bin/
/dist/
